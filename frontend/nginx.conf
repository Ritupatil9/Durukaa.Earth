server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: any unknown path falls back to index.html so
    # React Router can handle client-side routes like /projects/1.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
